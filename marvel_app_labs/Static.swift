import UIKit

struct Static {
    static let collectionViewLayoutInset = UIEdgeInsets(top: 0, left: 60, bottom: 0, right: 60)
    static let itemSpasing = 30.0
    static let collectionViewCardItemSize = CGSize(width: UIScreen.main.bounds.width - 100, height: UIScreen.main.bounds.height - 250)
}
